# Welcome to your prefect.yaml file! You can use this file for storing and managing
# configuration for deploying your flows. We recommend committing this file to source
# control along with your flow code.

# Generic metadata about this project
name: fx-rate-analytics
prefect-version: 2.19.5

# build section allows you to manage and build docker images
# build:
# - prefect.deployments.steps.run_shell_script:
#     id: build-1
#     script: sudo apt install wget
#     stream_output: false
# - prefect.deployments.steps.run_shell_script:
#     id: build-2
#     script: wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
#     stream_output: false
# - prefect.deployments.steps.run_shell_script:
#     id: build-3
#     script: sudo apt-get install -f
#     stream_output: false
# - prefect.deployments.steps.run_shell_script:
#     id: build-4
#     script: google-chrome --version
# - prefect.deployments.steps.run_shell_script:
#     id: build-5
#     script: rm google-chrome-stable_current_amd64.deb

# push section allows you to manage if and how this project is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning this project in remote locations
pull:
- prefect.deployments.steps.git_clone:
    id: clone-step
    repository: https://github.com/QuanPham19/fx-rate-analytics
    branch: main
- prefect.deployments.steps.pip_install_requirements:
    directory: "{{ clone-step.directory }}"
    requirements_file: requirements.txt
    stream_output: false
- prefect.deployments.steps.run_shell_script:
    script: "bash {{ clone-step.directory }}/chrome/install.sh"
    shell: bash
    # stream_output: false

# the deployments section allows you to provide configuration for deploying flows
deployments:
- name: fx-analytics-deployment
  version: null
  tags: []
  description: null
  entrypoint: main.py:run_exchange_rate_analytics
  parameters: {}
  work_pool:
    name: fx-process
    work_queue_name: null
    job_variables: {}
  schedules:
  - rrule: FREQ=WEEKLY;BYDAY=MO
    timezone: UTC
    active: true
